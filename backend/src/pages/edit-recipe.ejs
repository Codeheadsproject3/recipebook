<%- include('header'); -%>
<div class="container-form">
	<h1><% if(editing){ %>Edit Recipe<% } else { %>Add Recipe<% } %></h1>

	<form method="POST" action="/<% if(editing){ %>edit-recipe<% } else { %>add-recipe<% } %>">
		<div>
			<label>Name</label>
			<input type="text" name="name" value="<% if(editing){ %><%= recipe.name %><% } %>" required />
		</div>
		<div>
			<label>Image</label>
			<input type="text" name="image" value="<% if(editing){ %><%= recipe.image %><% } %>" required />
		</div>
		<div>
			<label>Description</label>
			<textarea type="text" name="description" rows="10" cols="50" required ><% if(editing){ %><%= recipe.description %><% } %></textarea>
		</div>
		<% if(editing){ %>
		<div>
			<input type="hidden" name="recipeId" value="<%= recipe.id %>" />
		</div>
		<% } %>
		<div>
			<button type="submit"><% if(editing){ %>Edit Recipe<% } else { %>Add Recipe<% } %></button>
		</div>
	</form>
</div>
		<%- include('footer'); -%>
	</body>
</html>
